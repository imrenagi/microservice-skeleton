spring:
  datasource:
    url: jdbc:mysql://localhost/auth-test
    username: root
    password: imrenagi
    driver-class-name: com.mysql.jdbc.Driver
    hikari:
      connection-timeout: 60000
      maximum-pool-size: 5

flyway:
  url: jdbc:mysql://localhost:3306/auth-test
  user: root
  password: imrenagi
  locations: classpath:db/migration
  enabled: true